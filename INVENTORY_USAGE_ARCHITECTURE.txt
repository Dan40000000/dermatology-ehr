╔═══════════════════════════════════════════════════════════════════════════════╗
║                    INVENTORY USAGE TRACKING ARCHITECTURE                      ║
╚═══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                              USER INTERFACE                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    AppointmentFlowPage.tsx
    ┌────────────────────────────────────────────────────┐
    │  📅 Appointment Flow Dashboard                     │
    │                                                     │
    │  ┌──────────────────────────────────────────────┐  │
    │  │  Appointment Detail Modal                    │  │
    │  │  ┌────────────────────────────────────────┐  │  │
    │  │  │  Patient Info | Schedule | Status      │  │  │
    │  │  └────────────────────────────────────────┘  │  │
    │  │  ┌────────────────────────────────────────┐  │  │
    │  │  │  📦 Inventory Used (if checked-in+)   │  │  │
    │  │  │  ┌──────────────────────────────────┐  │  │  │
    │  │  │  │  InventoryUsageList Component    │  │  │  │
    │  │  │  │  • Shows items used              │  │  │  │
    │  │  │  │  • Displays total cost           │  │  │  │
    │  │  │  │  • "+ Use Items" button          │  │  │  │
    │  │  │  └──────────────────────────────────┘  │  │  │
    │  │  └────────────────────────────────────────┘  │  │
    │  │  [Close] [Record Inventory] [Next Step]      │  │
    │  └──────────────────────────────────────────────┘  │
    └────────────────────────────────────────────────────┘
                          │
                          │ Click "Record Inventory"
                          ▼
    ┌────────────────────────────────────────────────────┐
    │  InventoryUsageModal Component                     │
    │  ┌──────────────────────────────────────────────┐  │
    │  │  Search: [____________] Category: [All ▼]    │  │
    │  ├──────────────────────────────────────────────┤  │
    │  │  ☐ Lidocaine 1%            Available: 50     │  │
    │  │  ☐ Triamcinolone 40mg      Available: 30     │  │
    │  │  ☑ Botox (100 units)       Available: 10     │  │
    │  │    Quantity: [2] Notes: [_______________]    │  │
    │  │  ☐ Liquid Nitrogen          Available: 5     │  │
    │  ├──────────────────────────────────────────────┤  │
    │  │  [Cancel]  [Record Usage (1)]               │  │
    │  └──────────────────────────────────────────────┘  │
    └────────────────────────────────────────────────────┘
                          │
                          │ Submit
                          ▼

┌─────────────────────────────────────────────────────────────────────────────┐
│                              API LAYER (Frontend)                            │
└─────────────────────────────────────────────────────────────────────────────┘

    api.ts
    ┌────────────────────────────────────────────────────┐
    │  recordInventoryUsage()                            │
    │  • POST /api/inventory/usage                       │
    │  • Payload: itemId, quantityUsed, patientId, etc  │
    │                                                     │
    │  fetchAllInventoryUsage()                          │
    │  • GET /api/inventory/usage?appointmentId=xxx      │
    │  • Returns: usage records with patient/provider    │
    │                                                     │
    │  deleteInventoryUsage()                            │
    │  • DELETE /api/inventory/usage/:id                 │
    └────────────────────────────────────────────────────┘
                          │
                          │ HTTP Requests
                          ▼

┌─────────────────────────────────────────────────────────────────────────────┐
│                           BACKEND API (Express.js)                           │
└─────────────────────────────────────────────────────────────────────────────┘

    routes/inventory.ts
    ┌────────────────────────────────────────────────────┐
    │  POST /api/inventory/usage                         │
    │  1. Validate request (Zod schema)                  │
    │  2. Check authentication & authorization           │
    │  3. Verify item exists and quantity available      │
    │  4. INSERT into inventory_usage table              │
    │  5. Trigger fires automatically ─────────┐         │
    │  6. Create audit log                     │         │
    │  7. Return success response              │         │
    │                                          │         │
    │  GET /api/inventory/usage                │         │
    │  • Filter by appointmentId/encounterId   │         │
    │  • JOIN with items, patients, users      │         │
    │  • Return enriched usage records         │         │
    │                                          │         │
    │  DELETE /api/inventory/usage/:id         │         │
    │  • Admin only                            │         │
    │  • Add quantity back to inventory        │         │
    │  • Delete usage record                   │         │
    └──────────────────────────────────────────┼─────────┘
                          │                    │
                          │ SQL Queries        │
                          ▼                    │
                                              │
┌─────────────────────────────────────────────┼───────────────────────────────┐
│                           DATABASE (PostgreSQL)                │             │
└────────────────────────────────────────────────────────────────┼─────────────┘
                                                                 │
    Tables:                                                      │
    ┌──────────────────────────────────────┐                    │
    │  inventory_items                     │                    │
    │  • id (PK)                           │                    │
    │  • name, category, sku               │                    │
    │  • quantity ◄─────────────────────────┼────┐              │
    │  • unit_cost_cents                   │    │              │
    │  • reorder_level, supplier, etc      │    │ Updates      │
    └──────────────────────────────────────┘    │ quantity     │
                    ▲                            │              │
                    │ References                 │              │
                    │                            │              │
    ┌───────────────┴──────────────────────┐    │              │
    │  inventory_usage                     │    │              │
    │  • id (PK)                           │    │              │
    │  • item_id (FK) ──────────────────────────┘              │
    │  • appointment_id (FK)               │                   │
    │  • patient_id (FK)                   │                   │
    │  • provider_id (FK)                  │                   │
    │  • quantity_used                     │                   │
    │  • unit_cost_cents (frozen)          │                   │
    │  • notes, used_at                    │                   │
    └──────────────────────────────────────┘                   │
                    │ INSERT triggers ──────────────────────────┘
                    │
    ┌───────────────┴──────────────────────┐
    │  inventory_adjustments               │
    │  • id (PK)                           │
    │  • item_id (FK)                      │
    │  • adjustment_quantity (+/-)         │
    │  • reason, notes, created_by         │
    └──────────────────────────────────────┘

    Database Triggers:
    ┌──────────────────────────────────────────────────────────┐
    │  trigger_decrease_inventory                              │
    │  AFTER INSERT ON inventory_usage                         │
    │  BEGIN                                                   │
    │    UPDATE inventory_items                                │
    │    SET quantity = quantity - NEW.quantity_used           │
    │    WHERE id = NEW.item_id;                              │
    │                                                          │
    │    IF quantity < 0 THEN                                 │
    │      RAISE EXCEPTION 'Insufficient inventory';          │
    │    END IF;                                              │
    │  END;                                                   │
    └──────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              DATA FLOW EXAMPLE                               │
└─────────────────────────────────────────────────────────────────────────────┘

    Scenario: Record 2 units of Botox for appointment

    1. User selects "Botox (100 units)", quantity: 2, notes: "Glabellar lines"
    
    2. Frontend → POST /api/inventory/usage
       {
         itemId: "uuid-botox",
         quantityUsed: 2,
         patientId: "patient-123",
         providerId: "doctor-456",
         appointmentId: "appt-789",
         notes: "Glabellar lines"
       }

    3. Backend validates:
       ✓ User authenticated
       ✓ User has role: provider
       ✓ Botox exists
       ✓ Current quantity: 10 (sufficient for 2)

    4. Database INSERT:
       inventory_usage:
         id: "new-uuid"
         item_id: "uuid-botox"
         quantity_used: 2
         unit_cost_cents: 42500 (frozen at $425.00)
         appointment_id: "appt-789"
         patient_id: "patient-123"
         provider_id: "doctor-456"
         notes: "Glabellar lines"
         used_at: 2026-01-05 19:45:00

    5. Trigger executes:
       UPDATE inventory_items
       SET quantity = 10 - 2 = 8
       WHERE id = "uuid-botox"

    6. Audit log created:
       Action: "inventory_usage_create"
       User: "doctor-456"
       Resource: "inventory_item/uuid-botox"

    7. Frontend receives:
       {
         id: "new-uuid",
         usedAt: "2026-01-05T19:45:00Z",
         message: "2 units of Botox (100 units) recorded"
       }

    8. InventoryUsageList refreshes and shows:
       ┌────────────────────────────────────────┐
       │ Botox (100 units)       [Cosmetic]    │
       │ Quantity: 2                            │
       │ Notes: Glabellar lines                 │
       │ 2026-01-05 7:45 PM          $850.00   │
       └────────────────────────────────────────┘
       
       Total Inventory Cost: $850.00

    9. Inventory page now shows:
       Botox (100 units): 8 units (was 10)

┌─────────────────────────────────────────────────────────────────────────────┐
│                              SECURITY LAYERS                                 │
└─────────────────────────────────────────────────────────────────────────────┘

    Layer 1: Authentication
    ├─ JWT token validation
    ├─ Session management
    └─ Tenant isolation

    Layer 2: Authorization (RBAC)
    ├─ Record usage: Admin, Provider, MA
    ├─ View usage: All authenticated
    └─ Void usage: Admin only

    Layer 3: Input Validation
    ├─ Zod schema validation
    ├─ Type checking (TypeScript)
    └─ Sanitization

    Layer 4: Database Constraints
    ├─ Foreign key references
    ├─ Check constraints (quantity > 0)
    └─ Trigger validations

    Layer 5: Audit Trail
    ├─ All actions logged
    ├─ User, timestamp, action recorded
    └─ Immutable audit log

╔═══════════════════════════════════════════════════════════════════════════════╗
║                           IMPLEMENTATION COMPLETE                             ║
║                                                                               ║
║  ✓ Backend API endpoints                                                     ║
║  ✓ Frontend components                                                       ║
║  ✓ Database schema & triggers                                                ║
║  ✓ Integration with appointment flow                                         ║
║  ✓ Security & validation                                                     ║
║  ✓ Comprehensive documentation                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝
