PORT=4000
JWT_SECRET=change-me
JWT_ISSUER=derm-app
ACCESS_TOKEN_TTL_SEC=900
REFRESH_TOKEN_TTL_SEC=1209600
TENANT_HEADER=x-tenant-id
DATABASE_URL=postgres://derm_user:derm_pass@localhost:5432/derm_db
DB_PASSWORD=derm_pass
DEFAULT_USER_PASSWORD=your-secure-default-password
STORAGE_PROVIDER=s3
AWS_S3_BUCKET=your-bucket
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
CLAMAV_HOST=localhost
CLAMAV_PORT=3310
CLAMAV_TIMEOUT_MS=4000
PHI_ENCRYPTION_ENABLED=false
ENCRYPTION_KEY=
INIT_SECRET=

# Twilio SMS Configuration (optional - for text messaging feature)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# AI Services for Ambient Clinical Notes (Optional - falls back to mock if not provided)
# OpenAI API key for transcription and note generation
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional model overrides (defaults shown)
OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe-diarize
OPENAI_NOTE_MODEL=gpt-4o

# Anthropic API key for Claude note generation (preferred for medical documentation)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=
# Optional model override (default shown)
ANTHROPIC_NOTE_MODEL=claude-3-5-sonnet-20241022
AMBIENT_LIVE_TRANSCRIBE_ENABLED=true
AMBIENT_LIVE_TRANSCRIBE_MIN_INTERVAL_MS=5000
AMBIENT_LIVE_TRANSCRIBE_MODEL=gpt-4o-transcribe
