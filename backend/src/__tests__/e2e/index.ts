/**
 * E2E Integration Tests Index
 *
 * This file serves as a reference for all E2E test suites.
 * Tests can be run individually or as a complete suite.
 *
 * Usage:
 *   npm test -- --testPathPattern=e2e                    # Run all E2E tests
 *   npm test -- timeBlocks.test.ts                       # Run specific suite
 *   npm test -- --testPathPattern=e2e --coverage         # With coverage
 *
 * Test Suites:
 *   1. timeBlocks.test.ts          - Time blocks with recurrence
 *   2. waitlistAutoFill.test.ts    - Waitlist matching and auto-fill
 *   3. priorAuthorization.test.ts  - Prior auth workflow
 *   4. fax.test.ts                 - Fax sending and receiving
 *   5. portalPreCheckin.test.ts    - Patient portal check-in
 *
 * See README.md for detailed documentation.
 */

export {};
