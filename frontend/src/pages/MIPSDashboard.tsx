/**
 * Compatibility entry point for older imports.
 *
 * MIPS work now lives in the authenticated MIPS Readiness Center. Keeping
 * this thin re-export avoids reintroducing the former fake submission UI.
 */
export { default } from './MIPSReadinessPage';
