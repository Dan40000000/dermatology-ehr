import React from 'react';
import ProfileScreen from '../patient/ProfileScreen';

// Provider profile uses the same component as patient profile
// Could be customized with provider-specific settings in the future
export default ProfileScreen;
